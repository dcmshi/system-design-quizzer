{
  "name": "system-design-quizzer-frontend-tests",
  "private": true,
  "type": "module",
  "description": "Test harness for the static quiz/review/sources pages.",
  "scripts": {
    "test": "node --test \"tests/js/**/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
